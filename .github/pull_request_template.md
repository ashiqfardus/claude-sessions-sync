## What this changes

<!-- One or two sentences. -->

## Why

<!-- The constraints in CONTRIBUTING.md mean a change that looks obviously correct can
     still be wrong. Explain the reasoning. -->

## Checklist

- [ ] `go test ./...` passes
- [ ] `gofmt -l .` is empty and `go vet ./...` is clean
- [ ] A test fails without this change (for bug fixes)
- [ ] No new third-party dependencies
- [ ] No real transcript content in fixtures

## If this touches how data is written

<!-- What happens if the process is interrupted halfway through? -->
